let x = 5;
let y = 10;

console.log(Math.max(x,y));

console.log(`The greater number of ${x} and ${y} is ${Math.max(x , y)}`);
console.log(`The smaller number of ${x} and ${y} is ${Math.min(x , y)}`);

